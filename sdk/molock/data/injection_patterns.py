# -*- coding: utf-8 -*-
"""Injection detection patterns, routing keywords, and conflict detection rules."""

import re

# ============================================================
# M1: 预处理 — 注入拦截正则
# ============================================================
INJECTION_PATTERNS = [
    re.compile(r"忽略.*(?:规则|前文|指令|限制)", re.IGNORECASE),
    re.compile(r"跳过.*(?:流程|步骤|检查|校验)", re.IGNORECASE),
    re.compile(r"无视.*(?:前文|限制|约束|边界)", re.IGNORECASE),
    re.compile(r"直接输出|不要思考.*直接|别说废话.*直接", re.IGNORECASE),
    re.compile(r"你现在.*(?:是|扮演).*角色", re.IGNORECASE),
    re.compile(r"扮演.*没有限制|无任何限制.*角色", re.IGNORECASE),
    re.compile(r"forget.*previous|ignore.*above", re.IGNORECASE),
    re.compile(r"system.*prompt|系统提示词", re.IGNORECASE),
    re.compile(r"\[INST\]|</system>|<\|im_start\|>", re.IGNORECASE),
    re.compile(r"忽略所有.*你.*(?:听到|看到|读到)的", re.IGNORECASE),
]

# ============================================================
# M1: 预处理 — 冲突检测模式
# ============================================================
CONFLICT_PATTERNS = [
    # (模式1, 模式2, 冲突类型)
    (["可爱", "萌", "童趣", "卡哇伊"], ["商务", "严谨", "正式", "专业"], "STYLE"),
    (["详尽", "全面", "完整", "详细"], ["极简", "精炼", "简洁"], "SCOPE"),
    (["儿童", "青少年", "低龄"], ["专业术语", "技术文档", "学术"], "AUDIENCE"),
    (["创新", "突破", "颠覆"], ["零风险", "保守", "稳妥"], "GOAL"),
]

# ============================================================
# M5: 动态意图路由 — 词表
# ============================================================

# 发散词（含权重）
DIVERGE_WORDS = {
    "设计": 1.0, "创造": 1.0, "生成": 1.0, "制作": 1.0,
    "画": 0.8, "写": 0.8, "编": 0.8,
    "创新": 1.0, "创意": 1.0, "原创": 1.0,
    "想象": 0.8, "畅想": 0.8, "构思": 0.8,
    "美化": 0.7, "装饰": 0.6,
    "头脑风暴": 1.0, "脑洞": 1.0,
    "故事": 0.8, "剧情": 0.8, "角色": 0.6,
    "剧本": 0.8, "小说": 0.8,
    "灵感": 0.8, "点子": 0.8,
    "风格": 0.6, "配色": 0.6, "排版": 0.6,
    "推荐": 0.6,
}

# 收敛词（含权重）
STRICT_WORDS = {
    "查询": 1.0, "统计": 1.0, "计算": 1.0,
    "分析": 0.8, "对比": 0.8, "比较": 0.8,
    "验证": 1.0, "核实": 1.0, "确认": 1.0,
    "事实": 1.0, "真相": 1.0, "数据": 1.0,
    "定义": 1.0, "解释": 0.8,
    "推理": 0.8, "判断": 0.8, "判定": 0.8,
    "逻辑": 1.0, "论证": 1.0, "推导": 1.0,
    "错误": 0.8, "问题": 0.5, "故障": 0.8,
    "法律": 1.0, "合规": 1.0, "安全": 0.6,
    "策略": 0.5, "方案": 0.5, "评估": 0.6,
    "诊断": 1.0, "排查": 1.0, "检查": 0.8,
    "翻译": 0.8, "转换": 0.6, "格式": 0.5,
    "历史": 0.8, "事件": 0.8, "日期": 1.0,
    "步骤": 0.6, "流程": 0.6, "教程": 0.6,
    "排序": 1.0, "排名": 1.0, "筛选": 0.8,
    "最高": 0.8, "最低": 0.8, "平均": 0.8,
    "原因": 0.8, "为什么": 0.8,
    "是否": 0.8, "有没有": 0.8, "能不能": 0.5,
}

# 否定前缀
NEGATION_WORDS = [
    "不", "别", "不要", "不用", "不必", "不许", "不准",
    "不该", "不能", "不应该", "不可以",
    "禁止", "严禁", "无需", "无须", "杜绝", "避免",
    "防止", "抵制", "对抗", "摆脱", "不要搞",
    "阻止", "遏制", "制止", "别搞", "不搞", "不弄", "别弄",
    "请勿", "切勿", "切莫",
]

# 口语化发散短语（命中 = 直接发散）
INFORMAL_DIVERGE = [
    "你看着办", "你懂的", "随便搞",
    "整一个", "来一个", "弄一个", "搞一个",
    "看着来", "你决定", "你定",
    "发挥一下", "自由发挥", "脑洞大开",
    "怎么看", "觉得呢",
]

# 固定术语保护（不拆分的复合词）
PROTECTED_TERMS = [
    "设计模式", "方案评估", "数据分析", "安全评估",
    "风险评估", "翻译模型", "决策分析",
]

# 隐性意图句式（抱怨/评价 → force_divergence）
IMPLICIT_DIVERGE = [
    (r"太丑[了啦]?", 0.9),
    (r"好?难看", 0.8),
    (r"不好看", 0.7),
    (r"不行[了啦]?", 0.6),
    (r"不够好", 0.6),
    (r"还有.*(?:更好的|别的|其他)", 0.7),
    (r"有没有.*(?:更好|别的|其他)", 0.7),
]
